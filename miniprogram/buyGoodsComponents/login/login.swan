<view s-if="{{show}}" style="opacity: {{opacity}}" class="container" catchtap="tapMask" catchtouchmove="moveMask">
    <view class="window">
        <view>
            <image class="img" src="./login.svg"></image>
            <button class="btn" open-type="getUserInfo" bindgetuserinfo="tapLogin">授权登录</button>
        </view>
    </view>
</view>